# theejs-site-builder

Minimal Three.js site builder demo.

Commands:

- npm run build  # runs node build.js to emit dist/
- npm test       # runs node test.js to validate build

The demo uses a CDN-hosted Three.js so no local install is required for the build output.
